[project]
name = "2015"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "z3-solver",
    "pyperclip",
    "sympy",
    "networkx",
    "requests",
]
